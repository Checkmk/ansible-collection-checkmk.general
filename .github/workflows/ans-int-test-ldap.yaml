concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

name: Module LDAP

permissions:
  contents: read

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'
  pull_request:
    branches:
      - main
      - devel
    types:
      - opened
    paths:
      - 'plugins/modules/ldap.py'
  push:
    paths:
      - '.github/workflows/ans-int-test-ldap.yaml'
      - 'plugins/modules/ldap.py'
      - 'plugins/module_utils/**'
      - 'tests/integration/files/includes/**'
      - 'tests/integration/targets/ldap/**'

jobs:

  integration:
    name: "Integration ${{ matrix.checkmk_version }}.${{ matrix.checkmk_edition }}"
    uses: ./.github/workflows/_template-ans-int-test.yaml
    with:
      module: ldap
      checkmk_version: ${{ matrix.checkmk_version }}
      checkmk_edition: ${{ matrix.checkmk_edition }}
    strategy:
      fail-fast: false
      matrix:
        checkmk_version:
          - 2.4.0p24
          - 2.5.0-2026.03.18
        checkmk_edition:
          - managed
          - ultimatemt
        exclude:
          - checkmk_version: 2.3.0p45
            checkmk_edition: ultimatemt
          - checkmk_version: 2.4.0p24
            checkmk_edition: ultimatemt
          - checkmk_version: 2.5.0-2026.03.18
            checkmk_edition: managed
