concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

name: Module Password Integration

permissions:
  contents: read

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'
  pull_request:
    branches:
      - main
      - devel
    types:
      - opened
    paths:
      - 'plugins/modules/password.py'
  push:
    paths:
      - '.github/workflows/ans-int-test-password.yaml'
      - 'plugins/modules/password.py'
      - 'plugins/module_utils/**'
      - 'tests/integration/files/includes/**'
      - 'tests/integration/targets/password/**'

jobs:

  integration:
    name: "${{ matrix.checkmk_version }}.${{ matrix.checkmk_edition }}"
    uses: ./.github/workflows/_template-ans-int-test.yaml
    with:
      module: password
      checkmk_version: ${{ matrix.checkmk_version }}
      checkmk_edition: ${{ matrix.checkmk_edition }}
    strategy:
      fail-fast: false
      matrix:
        checkmk_version:
          - 2.3.0p48
          - 2.4.0p31
          - 2.5.0p5
        checkmk_edition:
          - raw
          - managed
          - community
          - ultimatemt
        exclude:
          - checkmk_version: 2.3.0p48
            checkmk_edition: community
          - checkmk_version: 2.3.0p48
            checkmk_edition: ultimatemt
          - checkmk_version: 2.4.0p31
            checkmk_edition: community
          - checkmk_version: 2.4.0p31
            checkmk_edition: ultimatemt
          - checkmk_version: 2.5.0p5
            checkmk_edition: raw
          - checkmk_version: 2.5.0p5
            checkmk_edition: managed
