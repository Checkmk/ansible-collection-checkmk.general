concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

name: Module DCD Telemetry Metrics Integration

permissions:
  contents: read

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'
  pull_request:
    branches:
      - main
      - devel
    types:
      - opened
    paths:
      - 'plugins/modules/dcd_telemetry_metrics.py'
  push:
    paths:
      - '.github/workflows/ans-int-test-dcd_telemetry_metrics.yaml'
      - 'plugins/modules/dcd_telemetry_metrics.py'
      - 'plugins/module_utils/**'
      - 'tests/integration/targets/setup_checkmk/**'
      - 'tests/integration/targets/dcd_telemetry_metrics/**'

jobs:

  integration:
    name: "${{ matrix.checkmk_version }}.${{ matrix.checkmk_edition }}"
    uses: ./.github/workflows/_template-ans-int-test.yaml
    with:
      module: dcd_telemetry_metrics
      checkmk_version: ${{ matrix.checkmk_version }}
      checkmk_edition: ${{ matrix.checkmk_edition }}
    strategy:
      fail-fast: false
      matrix:
        checkmk_version:
          - 3.0.0-2026.09.23
        checkmk_edition:
          - ultimatemt
