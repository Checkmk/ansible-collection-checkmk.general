concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

name: Module Data Backend Integration

permissions:
  contents: read

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'
  pull_request:
    branches:
      - main
      - devel
    types:
      - opened
    paths:
      - 'plugins/modules/data_backend.py'
  push:
    paths:
      - '.github/workflows/ans-int-test-data_backend.yaml'
      - 'plugins/modules/data_backend.py'
      - 'plugins/module_utils/**'
      - 'tests/integration/targets/setup_checkmk/**'
      - 'tests/integration/targets/data_backend/**'

jobs:

  integration:
    name: "${{ matrix.checkmk_version }}.${{ matrix.checkmk_edition }}"
    uses: ./.github/workflows/_template-ans-int-test.yaml
    with:
      module: data_backend
      checkmk_version: ${{ matrix.checkmk_version }}
      checkmk_edition: ${{ matrix.checkmk_edition }}
    strategy:
      fail-fast: false
      matrix:
        checkmk_version:
          - 3.0.0-2026.09.23
        checkmk_edition:
          - ultimatemt
