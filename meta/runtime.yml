requires_ansible: '>=2.12.0'

action_groups:
  checkmk:
    - activation
    - bakery
    - contact_group
    - discovery
    - downtime
    - folder
    - host
    - host_group
    - rule
    - service_group
    - tag_group
    - user

plugin_routing:
  modules:
    activation:
      redirect: checkmk.general.activation
    bakery:
      redirect: checkmk.general.bakery
    contact_group:
      redirect: checkmk.general.contact_group
    discovery:
      redirect: checkmk.general.discovery
    downtime:
      redirect: checkmk.general.downtime
    folder:
      redirect: checkmk.general.folder
    host_group:
      redirect: checkmk.general.host_group
    host:
      redirect: checkmk.general.host
    rule:
      redirect: checkmk.general.rule
    service_group:
      redirect: checkmk.general.service_group
    tag_group:
      redirect: checkmk.general.tag_group
    user:
      redirect: checkmk.general.user
