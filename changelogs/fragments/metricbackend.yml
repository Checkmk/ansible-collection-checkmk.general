minor_changes:
  - metricbackend module - Add new module to manage the Metric Backend configuration in Checkmk.
    The module supports enabling and disabling the metric backend per site as well as configuring ports and memory limits.
