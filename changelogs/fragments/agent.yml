minor_changes:
  - Agent role - Enable automatically adding the folder a host will be put in. This is disabled by default.
    With this feature enabled, the folder you want to put your host in will be created if it does not exist.
