---
- name: "Install xinetd"
  become: true
  ansible.builtin.package:
    name: xinetd
    state: present

- name: "Enable xinetd"
  become: true
  ansible.builtin.service:
    name: xinetd
    state: started
    enabled: true
