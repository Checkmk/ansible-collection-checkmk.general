---
checkmk_var_test_sites:
  - version: "{{ checkmk_var_version | default(checkmk_var_version_local) }}"
    edition: "cre"
    site: "cre"
    port: "5000"
  - version: "{{ checkmk_var_version | default(checkmk_var_version_local) }}"
    edition: "cme"
    site: "cme"
    port: "5100"

checkmk_var_hosts:
  - name: test1.tld
    folder: "/"
  - name: test2.tld
    folder: "/"
  - name: test3.tld
    folder: "/"
  - name: test4.tld
    folder: "/"
  - name: test5.tld
    folder: "/"
